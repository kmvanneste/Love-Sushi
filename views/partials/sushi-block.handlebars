<li>
	{{name}}

	<button class="change-sushi" data-id="{{id}}" data-newsushi="{{prepared}}">
		{{#if prepared}}Click to eat!{{else}}Click to re-order!{{/if}}
	</button>

	<button class="delete-sushi" data-id="{{this.id}}">DELETE!</button>
</li>